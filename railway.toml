[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn bloodcoord.wsgi --bind 0.0.0.0:$PORT --workers 2"
healthcheckPath = "/admin/login/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
